{
  "name": "kktc-galeri-yonetim",
  "version": "1.0.0",
  "private": true,
  "description": "KKTC araç galerileri için multi-tenant yönetim paneli",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean && rm -rf node_modules .turbo"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.15.0"
  }
}
